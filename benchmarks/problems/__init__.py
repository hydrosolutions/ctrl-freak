"""Benchmark optimization problems (single- and multi-objective).

Lazy package: concrete problem modules (``single_objective.py``,
``multi_objective.py``) are added by later milestone steps; this file only marks
the directory as an importable package.
"""
