"""Fair cross-library benchmark harnesses.

Lazy package: harness modules (``single_objective.py``, ``multi_objective.py``)
that run ctrl-freak against pymoo and DEAP under an identical budget are added by
later milestone steps; this file only marks the directory as an importable
package.
"""
